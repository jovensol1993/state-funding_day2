소스코드를 만들때 public하나만 붙을 수 있다.
나머지 public 붙으면 error

Java SE Development Kit 8u261 Documentation - 자바 라이브러리(api)
패키지 -폴더(비슷한기능끼리묶어논거)

매인함수가없으면? - 메모리에 올리고 끝.
변수 - 데이터담는 그릇

ASCII는 영문 알파벳을 사용하는 대표적인 문자 인코딩
유니코드(Unicode)는 전 세계의 모든 문자를 컴퓨터에서 일관되게 표현하고 다룰 수 있도록 설계된 산업 표준

2진법 

네모한칸 1비트

실수 - 정수부, 실수부 나눠서 기억함 따라서 long(8) 보단 float(4)가 더 크다.

int + float -> float 타입으로나옴(데이터타입이 큰쪽으로)

기본 데이터 타입8개 

= : 연산자(대입)

리터럴 풀 - 순수히 데이터만 모아둔 그룹
	특징: 같은데이터는 한번만 기억

스텍 - 주소만변수가 기억 (ex 111 주소를 가진 10인데이터 int no1 =10;)

new 하면 heap에 데이터가 쌓인다(긴생명주기, 엄청큼)
데이터를 만들면 리터럴 풀에 쌓이고
스택엔 데이터의 주소만 기억함
대표상수 True false null

시프트 연산자 -> shift 2의 n승 곱해준다

증감(++,--), 쉬프트(<<, <<<) 연산자는 cpu안타고 메모리에서 태워버림
